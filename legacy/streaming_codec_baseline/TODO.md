# TODO

## 数据加载与样本利用
- [ ] 将当前 `max_seq_len` 截断策略改为“切窗展开”策略：长样本超过上限时，不直接丢弃尾部，而是按固定窗口（可选 overlap）拆成多条训练样本，提升数据利用率。
- [ ] 新增统计脚本：基于 EnCodec 推理生成的 cache 统计序列长度频数（length histogram / frequency table），用于决定 `max_seq_len` 与切窗策略。
- [ ] 对当前训练数据及标注做抽检：每个数据集随机抽样 10 条，人工核对音频、歌词/音素、时长/边界与控制字段的一致性，并记录问题样本清单。
